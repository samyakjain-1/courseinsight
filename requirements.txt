# API requests for fetching data from external services
requests>=2.31.0

# Reddit API wrapper for fetching Reddit posts
praw>=7.7.1

# Google Generative AI for course summarization
google-generativeai>=0.3.2

# Environment variable management
python-dotenv>=1.0.0

# Vector database for semantic search
chromadb>=0.4.18

# Sentence transformers for embedding generation
sentence-transformers>=2.2.2

# Scientific computing (dependency for sentence-transformers)
numpy>=1.24.0

# Machine learning library (dependency for sentence-transformers)
torch>=2.0.0

# Additional ML dependencies
scikit-learn>=1.3.0
transformers>=4.35.0

# JSON handling is built-in to Python
# Standard library modules used: json, os, time, re, threading, signal, sys, concurrent.futures, datetime 